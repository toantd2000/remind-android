# Đặc thù Dự Án (Project-Specific Conventions)

Tài liệu này chứa các quy tắc, workarounds, và business logic **bất di bất dịch và chỉ áp dụng duy nhất cho dự án này**.
Các quy tắc trong này có quyền ưu tiên cao nhất, nó sẽ **GHI ĐÈ (OVERRIDE)** mọi tiêu chuẩn tốt (best practices) thông thường từ các `.skills` bên ngoài nếu có xung đột.

> Do Agent tự động cập nhật qua workflow `/learning-journal` mỗi khi phát hiện một quy tắc "chỉ-có-dự-án-này-mới-thế".

---

## 1. Domain & Business Rules
*(Agent sẽ tự động liệt kê vào đây)*

## 2. API & Naming Workarounds
*(Ví dụ: Backend trả date format khác thường, Key API viết hoa...)*

## 3. UI/UX Project Rules
*(Các cấu hình dành riêng cho design project này, không phải chuẩn Material)*
